# Evidence record: `<gate-id>`

**Status**: passed | failed | blocked  
**Started (UTC)**: YYYY-MM-DDTHH:MM:SSZ  
**Finished (UTC)**: YYYY-MM-DDTHH:MM:SSZ  
**Commit/worktree**: SHA plus `clean` or a concise dirty-file list  
**Operator**: human/automation identifier

## Scope and requirement

Describe the behavior proved and the explicit exclusions. Link the specification section and
capability IDs.

## Environment

| Item | Value |
| --- | --- |
| OS/architecture | |
| CPU/RAM limits | |
| Node/Python/uv/Docker versions | |
| Dataset fixture/version/size | |
| Tenant/role (fictional identifier only) | |
| Provider/source mode | deterministic double / local / live configured |
| Non-secret configuration | |

## Exact command or manual procedure

```sh
# command exactly as run
```

## Result

- Exit code:
- Tests/assertions attempted:
- Passed / failed / skipped / timed out:
- p50/p95/runtime/peak memory where relevant:
- Output artifact and SHA-256:
- Correlation/result/job/version IDs (non-sensitive):

## Inspection

For screenshots/exports, record route, locale, direction, theme, viewport, zoom, keyboard path,
expected state, visible defects, console errors, Arabic shaping, chart/table equivalence, and reviewer
decision. A capture without inspection is not a pass.

## Failures and limitations

Include the exact failing assertion or named blocker. Do not omit difficult in-scope cases from the
denominator. State whether the result invalidates any current capability claim.

## Follow-up

Owner, concrete next action, and capability/evidence entries to update.

