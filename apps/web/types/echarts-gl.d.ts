// echarts-gl ships no type declarations. It is imported purely for the side effect of
// registering the 3D chart types onto the echarts instance, so an empty module
// declaration describes it exactly.
declare module "echarts-gl";
